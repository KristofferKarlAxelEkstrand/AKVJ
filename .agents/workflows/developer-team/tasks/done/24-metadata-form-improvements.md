# Task 24: Metadata Edit Form Improvements

## Objective
Improve the metadata edit form and library UX in mainframe.

## Requirements
1. **PNG filename existence check**: When editing metadata, warn if the `png` filename doesn't exist in the clip directory
2. **Clip sort dropdown**: Add sort options in Library tab (by name, size, role)
3. **Batch operations**: Consider adding batch operations if they fit naturally

## Dependencies
None

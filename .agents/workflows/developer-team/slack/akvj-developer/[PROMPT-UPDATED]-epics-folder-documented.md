[PROMPT-UPDATED]

File changed: `.agents/workflows/developer-team/spec/collaboration-protocol.md` (shared file — your own `akvj-developer.prompt.md` was not touched).

## What changed
Added a new row to the "The Mailboxes" table documenting `epics/` (e.g. `epics/refactor-for-greateness.md`) — Team Lead's planning artifact for large multi-task initiatives (like the refactor-audit tasks you may get assigned). This folder was already in use; the protocol just didn't mention it. No behavior change.

## Why
Small documentation gap found during a routine process sweep, now closed — since you should stay in sync with `collaboration-protocol.md` changes per the shared-file notify rule.

## What to do
1. Delete this notice.
2. Gracefully exit your process (`exit 0`). You'll restart with the updated context.

[PROMPT-UPDATED]

File changed: `.agents/workflows/developer-team/spec/collaboration-protocol.md` (shared file).

## What changed
Added a new row to the "The Mailboxes" table documenting `epics/` (e.g. `epics/refactor-for-greateness.md`, `epics/refactor-for-greateness-2.md`) — this folder was already in active use as your planning artifact for large multi-task initiatives, but the protocol never mentioned it. No behavior change, just catching the docs up to reality.

## Why
Noticed during a routine process sweep that `epics/` exists and is being read/written as part of the refactor-audit tasks (150/151) but wasn't in the mailbox table anywhere — small documentation gap, now closed.

## What to do
1. Delete this notice.
2. Gracefully exit your process (`exit 0`). You'll restart with the updated context.

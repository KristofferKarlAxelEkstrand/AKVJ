# Server Architect Inbox

Drop new feature requests, ideas, or bug reports here as `.md` files (e.g. `upload-images-to-create-clips.md`).

When the Server Architect agent runs, it will read this folder, automatically process your requests into actionable steps inside the `tasks/` directory, and move your original file to the `read/` folder.

# Team Update: Remove MCP configurations

## Summary
We currently do not have any active MCPs in use for the project.

## Impact
We can simplify our repository configuration and remove unneeded files.

## Action Needed
Please remove the settings and configuration files for MCPs (such as `.mcp.json` or any related `.vscode` configs) since they are no longer required.

## Notes
- Context: The user noted that we do not have any MCPs.
- Target files to review for deletion include `.mcp.json` in the root directory.

# Team Update: Default preview should be 1x

## Summary
The clip editor staging preview should default to **1x** playback speed.

## Impact
Preview speed is for editor preview only; it should open at real-time (1x), not a faster/slower default.

## Action Needed
Ensure the preview playback speed control and its initial state default to **1x**.

## Notes
- Related earlier note: `inbox/read/question-editor-playback-speed-default.md` (Team Lead had confirmed code already defaults to 1x — re-check if live UI still doesn’t).

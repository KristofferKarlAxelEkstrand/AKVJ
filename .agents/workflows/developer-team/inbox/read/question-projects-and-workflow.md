---
description:
---

Question: I'd like some background on how you actually use AKVJ live, so the team can design Task 73 ("Project" concept for grouping clips/MIDI mappings) around your real workflow instead of guessing.

A few things would help:
1. Right now the app has one global, flat set of clips + one key-map. Do you already think of your work as separate "sets" (e.g. one per gig, one per song, one per client) that you'd want to switch between, or is it more that everything lives in one big always-available bank and you just play different subsets of it live?
2. If you do want separate Projects: would you ever want to be mid-performance and hot-swap to a different project without a reload/restart, or is switching projects always something that happens beforehand (setup/soundcheck), never live?
3. Do things like your bitmask/mixer clips (Channel 5/10/13 effects) tend to be reusable across every gig, or are they usually custom per show too? This affects whether Projects should share those or each have their own.

No rush — whatever context you can give will help me update `spec/goal.md` and unblock Task 73's implementation questions. Also, separately: if there's a "code of conduct" or working style for you personally (vs. the code-of-conduct.prompt.md I already found, which covers JS code style) that the team should know about, let me know — otherwise I'll assume that doc is the full picture.

Answer:

1. Yes i think probably sets, I believe it should be one per gig or like project, but as you say it would make sense to also do it song by song pretty easy way to organize things so i dont know how we do it but I think maybe we should have a way to be able to even switch sets from the keyboarsd from keys so one channel is dedicated to what song is selected, I do not know really can you help me out here? I think i would like one big bank for a whole show, but I get it if someone would do it song for song. So we need som good naming.

Project
-- Mapping
-- Clips
-- Uploaded raw files
-- Settings for the project
General
-- Settings

And here I am a bit of like maybe some things cant be done on like a project level. Some things are general settingss.. Not set on the project.

2. I would kinda like thinking that switching should be able but maybe a bit of load time to get it ready I would believe? But yes i think you should be able by a press of a key switch it.

3. The bitmasks should be per project i think, I would think we will hae a standard but i think the user should be able to change it really. So i think they tend to be the same but they should be changeale and have your own and change it.

maybe this helsp please ask more questions!


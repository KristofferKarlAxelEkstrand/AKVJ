/**
 * Shared constants for pixel-level visual effects.
 */

/** Maximum MIDI velocity value (0-127). */
export const MAX_MIDI_VELOCITY = 127;

/** Maximum MIDI note value (0-127). */
export const MAX_MIDI_NOTE = 127;

/** Number of channels in an RGBA pixel (red, green, blue, alpha). */
export const RGBA_CHANNEL_COUNT = 4;

/** Number of color channels in an RGB pixel (red, green, blue, no alpha). */
export const RGB_CHANNEL_COUNT = 3;

/** Maximum value for an 8-bit color channel. */
export const MAX_COLOR_VALUE = 255;

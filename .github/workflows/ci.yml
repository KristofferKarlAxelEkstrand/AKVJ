name: CI

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

jobs:
    lint-and-build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
                  cache: 'npm'

            - name: Install dependencies
              run: npm ci

            - name: Lint JavaScript
              run: npm run lint

            - name: Lint CSS
              run: npx stylelint "**/*.css"

            - name: Check formatting (prettier)
              run: npx prettier --check .

            - name: Check formatting (npm script)
              run: npm run format:prettier -- --check

            - name: Run tests
              run: npm run test

            - name: Build
              run: npm run build

            - name: Generate animation metadata
              run: npm run generate-animation-json-to-json
